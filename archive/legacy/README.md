# 旧版 ARPG 模块归档

本目录存放已从主入口移除的 **单人实时 ARPG** 相关 JavaScript 模块（铁匠/商店/秘境/训练场/旧塔爬层等）。

现行游戏主循环为 **四人自走棋 Roguelike**，见仓库根目录 `index.html` 与 `js/auto-battler-*.js`。

如需参考或恢复旧逻辑，可从 `js/` 移回对应文件并在 `index.html` 中重新引入脚本标签。

归档日期：2026-07-26
